#!/bin/bash
DIR=$HOME/lib/audio/
mnt=$DIR.mnt/

Welcome() { \clear && echo -e "\n  Welcome to audio. \n \n" && sleep .3
}
MainMenu() { pwd && ls -a && tree -L2
}

a() { cd $DIR
}
b() { cd .bin
}
e() { cd exports
}
r() { cd .archive
}
s() { cd .source
}
t() { cd .tracking
}
z() { cd .ZOOM
}
zoom() { sudo mount /dev/sdb1 $mnt && cd $mnt/STEREO/FOLDER01
		echo "Don't forget to umount!" && sleep 1
}

Tracking() {
cd .tracking
PS3="  What track would you like to work on?"
select f in $(ls) ; do
$EDITOR $f
done
}
#tree -aL5


Welcome
MainMenu
